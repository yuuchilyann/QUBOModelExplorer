# Normalise line endings so the repo does not appear wholly modified when
# checked out on a different platform.
* text=auto eol=lf

# Binary / generated assets that must not be touched.
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
*.ttf binary
package-lock.json -diff linguist-generated=true
