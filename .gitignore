node_modules/
dist/
*.tsbuildinfo

# `publish/` is intentionally NOT ignored: the Pages workflow serves it
# directly from the repository, so the built site is committed alongside source.
.DS_Store

# Local editor / tooling configuration — machine-specific, not project content.
.vscode/
.idea/
.claude/
*.local
.env
.env.*
!.env.example
